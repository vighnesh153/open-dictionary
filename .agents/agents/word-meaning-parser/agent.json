{
  "name": "word-meaning-parser",
  "description": "An agent that parses the raw information about a word and writes the parsed information to a local file.",
  "configPath": {
    "relativePathToConfig": "config.yml"
  }
}